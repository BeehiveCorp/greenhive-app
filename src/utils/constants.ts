export enum TOAST_VARIANTS {
  Success = 'success',
  Error = 'error',
  Warning = 'warning',
  Info = 'info',
}

export const SPLASH_DURATION_IN_MILLISECONDS = 500;

export enum LEVEL_UP_DIFFICULTY_FACTOR {
  Easy = 1,
  Medium = 2,
  Hard = 3,
}
