// Each item in the bottom tab should contain the following width and height
export const BOTTOM_TAB_ITEM_SIZE = 70;

// Quantity of items in the bottom tab
export const BOTTOM_TAB_QUANTITY = 3;

// The bottom tab should contain the the following border width
export const BOTTOM_TAB_BORDER_WIDTH = 1;

// The bottom tab should contain the width of all items
export const BOTTOM_TAB_WIDTH = BOTTOM_TAB_ITEM_SIZE * BOTTOM_TAB_QUANTITY;
